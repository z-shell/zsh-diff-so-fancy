#!/usr/bin/env sh

# Git will search $PATH for this file, when invoked by "git dsf ...".

f() {
  [ -z "${GIT_PREFIX}" ] || builtin cd "${GIT_PREFIX}" && git diff --color "$@" | diff-so-fancy --patch | less --tabs=4 -RFX
}
f "$@"
